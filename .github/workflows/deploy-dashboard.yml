name: Deploy dashboard to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "dashboard/**"
      - "detector/**"
      - "data/**"
      - ".github/workflows/deploy-dashboard.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Generate graph data
        run: python3 detector/detector.py

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - name: Install & build
        working-directory: dashboard
        env:
          GITHUB_PAGES: "true"
          VITE_CHAIN_ID: "10143"
          VITE_MONAD_RPC: "https://10143.rpc.thirdweb.com"
          VITE_EXPLORER_URL: "https://testnet.monadvision.com"
          VITE_SYBIL_REGISTRY: "0x1D582E8d297d47273B64B80BD93c159513FD97D9"
          VITE_SYBIL_AIRDROP: "0x290fbF9fb7e06dF88A8C14546939db476888d9C4"
          VITE_PROBE_WALLET: "0xd8A08DF92652fF3992545D40DeDEe0FDA31f8871"
        run: |
          npm ci
          npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dashboard/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
